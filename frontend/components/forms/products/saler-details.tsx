import React from 'react'
import { useFormContext } from 'react-hook-form'

export default function SalerDetail() {
    const {control} = useFormContext()
  return (
    <div>saler-details</div>
  )
}
